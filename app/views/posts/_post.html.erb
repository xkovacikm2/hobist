<div class="post">
  <div class="postpanel">
    <%= link_to post do %>
        <div class="heading">
          <h4 class="title"><%= post.name %></h4>
          <p><b>Dátum:</b> <%= DateTime.strptime post.time.to_s, '%s' %></p>
          <p><b>Miesto:</b> <%= post.city.name %> : <%= post.locality %></p>
        </div>

        <div class="body">
          <% if post.picture.present? %>
            <%= image_tag post.picture.url %>
          <% else %>
            <p><%= strip_tags post.description[0,200] %></p>
          <% end %>
        </div>
    <% end %>
  </div>
</div>